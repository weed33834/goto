# FAQ

> 本文档面向当前 Goto **纯浏览器 Web 应用**(仓库 `desktop/`:Vite + React 18 + Zustand 4 + TypeScript 5,
> 数据存 IndexedDB,密钥经 Web Crypto / argon2id 派生)。

## Running it

**`npm run dev` 在哪个端口?**

Vite 默认 `5173`。如果端口被占用,Vite 会自动切到 `5174` / `5175` 等,看终端打印的实际地址即可。

**dev server 报 "EMFILE: too many open files"(macOS)。**

调高文件描述符上限,然后重启 dev server:
```bash
ulimit -n 65536
```

**全新安装后 `npm run typecheck` 报缺类型。**

`pnpm install` 后通常一次过。如果还失败,删 `node_modules` 与 lockfile 重装:
```bash
rm -rf node_modules pnpm-lock.yaml && pnpm install
```

**dev server 离线可用吗?**

首次 `pnpm install` 后,dev server 与构建链路都可离线跑。Web Audio / Web Crypto 等
浏览器 API 第一次使用时不需要联网。

## 主密码与解锁

**我忘了主密码,能找回吗?**

不能。本机仅以 argon2id (m=64MB t=3 p=4) 派生形式保存验证器,
没有服务端、没有重置邮件、没有后门。如果你曾经导出过加密备份并记得备份密码,
可以卸载应用 → 清空 IndexedDB → 重新设置主密码 → 用旧备份恢复。如果两者都没了,数据就没了。
这是本地优先加密的代价,请把主密码离线写下来。

**怎么修改主密码?**

设置页 → 安全 → 修改主密码 → 点"修改"展开 inline 表单。需要:
1. 当前密码(验证身份)
2. 新密码(≥8 位,且不能与当前密码相同)
3. 确认新密码

修改后,**此前生成的加密备份仍用旧密码恢复** —— 新密码只保护此后新生成的备份。建议改完
顺手重新导出一份加密备份。

**密码错误次数太多被锁了。**

连续 3 次输错密码会触发 30 秒 cooldown,期间拒绝任何解锁尝试。这是防暴力枚举的设计,
等 30 秒后用正确密码解锁即可,计数清零。

**密码输错次数会被攻击者用来 DoS 吗?**

会有一点点。但 30 秒 cooldown 是固定窗口,不会因持续错误而无限延长,等过了 30 秒就能再试
一次正确密码。设计上接受这个小 DoS 风险,换取暴力枚举难度大幅上升。

## 数据

**我的数据存在哪?**

IndexedDB,键空间按业务分库(`tasks` / `vault` / `projects` / `categories` / `tags` /
`app_settings` / `security_settings` 等)。密钥派生用 argon2id (m=64MB t=3 p=4),
保险库字段用 AES-256-GCM 加密。**清浏览器站点数据 = 清空所有 Goto 数据**,操作前请先导出备份。

**清浏览器数据后什么都没了,怎么办?**

抱歉,这是本地优先的代价。设置 → 数据 → 导出加密备份是唯一保险。备份文件用主密码加密,
可以放到云盘 / U 盘 / 邮件草稿里。恢复时设置 → 数据 → 导入备份,输入备份时的密码即可。

**清空数据 和 恢复出厂设置 有什么区别?**

设置 → 危险区:

- **清空数据**:删除任务 / 保险库 / 项目 / 分类 / 标签 / 搜索历史 / 同步设备身份。**保留主密码与
  已生成的备份文件**,下次启动仍用当前密码解锁。
- **恢复出厂设置**:在清空数据基础上,**额外删除主密码与安全设置**,1 秒后自动 reload 回到
  首次安装状态。**已生成的加密备份文件不受影响**,仍可用旧密码恢复。

两者都需要二次确认,且都不可撤销。

**两台设备能同步吗?**

可以。Web 端 ⇄ Web 端通过端到端加密同步,可选自托管 relay 做 NAT 穿透兜底。配对是一次性
8 位码,5 分钟 TTL。配对成功后,LAN 直连优先,relay 兜底。relay 永远只看到密文。
协议栈见 [ARCHITECTURE.md](ARCHITECTURE.md)。

## 保险库与剪贴板

**复制保险库密码后,剪贴板多久清空?**

默认 30 秒。复制时按钮显示倒计时,30 秒后自动调用 `clearClipboard()`。
若在 30 秒内又复制了另一个字段,计时重新开始,只清最后一次复制的字段。

**我能改剪贴板清除时长吗?**

可以,在安全设置中调整 `clipboardClearSeconds`。设为 0 表示不清除(不推荐)。

**为什么 Web 端没有 OS 级截图保护?**

浏览器无法阻止 OS 截图工具(QuickLook / Snipping Tool / Flameshot)捕获页面内容。
设置页的"截图/录屏保护"开关只在桌面壳(Chromium 内核)内生效。Web 端用户需自行注意物理
环境安全,或使用桌面壳版本。

## 快捷键

**有哪些快捷键?**

应用内任意位置按 `?` 弹出快捷键浮层,或设置页 → 快捷键 → "查看所有快捷键"。

当前注册的快捷键:

| 组合 | 说明 |
|---|---|
| `?` | 显示快捷键帮助浮层 |
| `Mod + L` | 立即锁定应用(macOS Cmd+L,Windows/Linux Ctrl+L) |
| `Mod + B` | 折叠 / 展开侧栏 |
| `Mod + K` | 跳到搜索 |
| `/` | 跳到搜索(输入框更轻量触发) |
| `Mod + N` | 跳到今日任务并聚焦新建输入框 |
| `Esc` | 关闭弹层 / 取消编辑 |

**在输入框里按 `/` 或 `?` 没反应。**

设计如此。输入框聚焦时,只有 `Esc` 与带 `Mod` 的组合仍生效,单字符快捷键被屏蔽以免影响输入。

## 外观与无障碍

**字太小 / 太大怎么办?**

设置 → 外观 → 字体大小:小(紧凑)/ 中(默认)/ 大(易读)。通过 root font-size 等比例缩放
所有 rem-based 元素,适合高分屏放大或小屏紧凑。

**有深色模式吗?**

有。设置 → 外观 → 主题:浅色 / 深色 / 跟随系统。

## 同步与配对

**配对一直失败。**

九成是时钟偏差。relay 拒绝时间戳偏差 ±60 秒以外的握手。两台机器都 NTP 同步一下再试。
其他原因:
- relay URL 写错(必须含 `ws://` 或 `wss://` scheme)
- 跨网络时其中一端到 relay 的防火墙不通
- 8 位配对码过期(5 分钟 TTL,重新生成一个)

**保险库能同步到另一台设备吗?**

可以。保险库条目走和任务一样的 E2EE 通道。
JSON 明文导出**不含保险库**,避免敏感字段通过不安全通道泄露。

## 看起来像 bug 但其实不是

**TaskCard 在过去日期上显示"今天"/"明天"。**

逾期逻辑只在任务有 `dueTime` 且当前时间晚于该时间时触发。只有 `dueDate`(日期)的任务
不会进入逾期状态。比如"今天到期"的任务在早上 9 点不算逾期,这是有意为之,但会让人疑惑。

**搜索历史关掉应用就没了。**

不是 bug,设计如此。搜索历史目前存在 store 内存中,不持久化。
后续会加 `searchHistory` slice 持久化,见 ROADMAP。

**Web 端没有语音输入。**

Web Speech API 仅在 Chromium 内核浏览器中可用,且需要联网(第一次)。Firefox / Safari
不支持。这是 Web 平台限制,不是代码问题。

## 构建与部署

**build 产物多大?**

`desktop/dist/renderer/` 是纯静态 SPA,首屏 JS gzip ~103KB(远低于 250KB 预算),
包含 React + Zustand + framer-motion + 业务代码。丢到任意静态主机(Netlify / Vercel /
S3 + CloudFront / GitHub Pages)即可。
