# Privacy Policy — Goto

> 本文档为 Phase A 阶段最小可用版本,Phase B' 末完成 DPIA 后将发布 v1.0 正式版。
> 对应规划:`docs/GOTO_PIVOT_PLAN.md` §12.1 / §12.5。
> 适用对象:Goto Web App(浏览器 / PWA)、桌面壳(Chromium 内核)、Relay 自部署用户。
> 最近更新:2026-07-20

---

## 1. 我们是谁 — 数据控制者与处理者

Goto 采用 **本地优先 + 端到端加密(E2EE)** 架构,这与传统云端任务管理工具有本质区别。

| 角色 | 主体 | 实际能力 |
|---|---|---|
| **数据控制者(Controller)** | **用户本人** | 用户对自己生成的全部数据(任务、笔记、保险库、时间织锦派生数据)拥有完整控制权 |
| **数据处理者(Processor)** | Relay 中继服务器 | 仅转发密文数据包,无主密钥,**技术上无法解密内容**;可由用户自部署 |
| **数据处理者(Processor)** | Goto 官方(可选) | 仅在用户选择官方 Relay 时承担转发角色;同样无主密钥 |

### 1.1 数据流向

```
用户设备(明文) → 本机 Web Crypto 加密 → IndexedDB(密文)
                                      ↓
                              E2EE 密文同步(可选)
                                      ↓
                            Relay(只见密文,无密钥)
```

- 应用启动后,主密钥仅派生在内存中(Session 派生缓存),关闭页面 / 锁屏后清除。
- 同步链路上的 Relay 服务器接收的是端到端加密包,即使被攻击者完全接管也无法还原明文。
- Relay 不保存用户身份信息,仅记录设备 ID、连接时间戳等路由元数据(详见 §3.2)。

---

## 2. 我们收集什么数据

### 2.1 本地数据(永不离开设备,除非用户主动同步)

| 类别 | 字段示例 | 加密方式 |
|---|---|---|
| 任务数据 | 标题、描述、优先级、状态、截止日期、子任务、附件元数据 | 主密钥派生 AES-256-GCM |
| 保险库条目 | 自定义字段、加密文本、生成密码(可选) | 主密钥派生 AES-256-GCM |
| 时间织锦派生数据 | `MosaicTile`(从已完成任务派生:completedAt / priority / categoryId → emotion / shape / color) | 不独立存储,从任务派生 |
| 用户偏好 | 主题、布局、快捷键、隐私模式开关 | IndexedDB 明文存储(非敏感) |
| 设备身份 | Ed25519 私钥(用于 P2P 同步鉴权) | 主密钥派生加密存储 |

### 2.2 同步元数据(仅用户启用同步时)

| 字段 | 用途 | 保留期 |
|---|---|---|
| `deviceId` | 区分配对设备 | 直到用户主动断开 |
| 连接时间戳 | 排查连接问题 | 30 天滚动 |
| 配对码(6 位) | 一次性设备配对 | 配对完成或 10 分钟过期 |
| 转发字节数 | 自部署 Relay 流量统计(可选) | 用户自行决定 |

### 2.3 不收集的数据(明确声明)

- ❌ **不收集**姓名、邮箱、电话(无账号系统)
- ❌ **不收集**位置信息、IP 地址(同步时 Relay 可见 IP,但官方 Relay 默认不记录)
- ❌ **不收集**设备指纹、浏览器指纹
- ❌ **不收集**使用行为埋点、漏斗数据
- ❌ **不收集**关系链、通讯录
- ❌ **不上传**任务内容、附件、笔记、保险库任何条目

### 2.4 关于"心理 / 情绪"类特殊数据(Phase B' 末 DPIA 决定)

规划中已识别 `MosaicTile.emotion` 字段、Delta 曲线、生灵权重可能构成 GDPR 第 9 条定义的"特殊类别数据"(心理 / 情绪画像)。处理原则:

- **Phase A 阶段**:`emotion` 字段仅作为本地派生计算,**不上传、不持久化为独立字段**,用户可在设置中关闭派生计算。
- **Phase B' 末**完成 DPIA(数据保护影响评估)前,该字段不进入跨设备同步。
- **DPIA 不通过**则该字段永久删除,不会以任何形式恢复。
- 即便 DPIA 通过,用户必须 **明示同意(Article 9(2)(a))** 才会启用,且 14 天内可随时撤回,撤回后不影响核心功能使用。

---

## 3. 用户权利(GDPR 第 12-22 条 + PIPL 第 44-50 条)

Goto 的本地优先架构让大部分权利天然在本地完成,无需发起服务器请求。

| 权利 | 落地方式 |
|---|---|
| **访问权** | 打开应用即可查看全部本地数据;导出 JSON 备份即可获取完整副本 |
| **可携带权** | 设置 → 备份 → 导出 JSON(加密或明文,用户选择)|
| **更正权** | 直接编辑任务 / 笔记 / 保险库条目 |
| **删除权 / 被遗忘权** | 删除单条数据或清空全部数据;启用同步时主动断开 Relay 连接,Relay 离线队列 7 天 TTL 自动过期 |
| **限制处理权** | 关闭同步、关闭 emotion 派生、关闭时间织锦显示 |
| **反对权** | 卸载应用 = 反对一切处理 |
| **自动化决策反对权** | Goto 不做云端自动化决策(详见 §4) |
| **撤回同意** | 设置中可撤回同步同意、emotion 派生同意、分享码生成同意 |

### 3.1 行使权利的具体路径

```text
设置 → 隐私与安全
  ├── 导出全部数据(JSON,加密 / 明文)
  ├── 清空本地数据(需主密码确认)
  ├── 断开所有同步设备
  └── 查看本地处理日志(最近 7 天的派生计算记录)
```

### 3.2 响应时间

- 本地操作:即时
- 同步断开:即时(Relay 收到断开指令后 7 天 TTL 内清除队列)
- 投诉响应:7 个工作日内回复(详见 §8 联系方式)

---

## 4. 我们如何使用数据(法律基础)

Goto 的处理活动按 GDPR 第 6 条 / 第 9 条分类:

| 处理活动 | 法律基础 | 必要性 |
|---|---|---|
| 本地存储任务 / 笔记 / 保险库 | 第 6(1)(b) 合同履行(用户使用 App 即合同) | 必要 — 不存储则无法使用 |
| 本机加密派生主密钥 | 第 6(1)(b) 合同履行 | 必要 — 不加密则无法保障数据安全 |
| 可选 P2P 同步 | 第 6(1)(a) 明示同意 | 可选 — 不同步不影响本地使用 |
| 时间织锦派生计算 | 第 6(1)(b) 合同履行 | 可选 — 用户可关闭 |
| **emotion 字段处理(若启用)** | **第 9(2)(a) 明示同意** | 可选 — 不勾选也能用产品核心功能 |
| 故障排查日志(本机) | 第 6(1)(f) 合法利益 | 可选 — 用户可关闭 |

### 4.1 我们不做的事

- ❌ 不做用户画像用于广告
- ❌ 不做行为分析用于推荐
- ❌ 不做云端 AI 训练
- ❌ 不向第三方出售数据
- ❌ 不与第三方共享数据(除 §5 法律义务)

---

## 5. 数据共享与跨境

### 5.1 共享对象

| 对象 | 共享内容 | 法律基础 |
|---|---|---|
| 用户自部署的 Relay | E2EE 密文 + 路由元数据 | 第 6(1)(a) 同意 |
| 官方 Relay(若用户选择) | 同上 | 第 6(1)(a) 同意 |
| 支付服务商(Phase D+ 接入) | 仅交易必要信息(订单号、金额) | 第 6(1)(b) 合同履行 |
| 执法机关(详见 TERMS.md §6) | 仅元数据(设备 ID、时间戳);**密文内容无法提供** | 第 6(1)(c) 法律义务 |

### 5.2 跨境传输

- 用户自部署 Relay:数据不出用户选择的司法管辖区
- 官方 Relay 默认部署在欧盟 + 美东,用户可在设置中切换区域
- 不同区域版本默认使用对应区域的 Relay,避免数据跨境

---

## 6. 数据安全

### 6.1 加密方案

| 层 | 算法 | 参数 |
|---|---|---|
| 主密钥派生(KDF) | argon2id(hash-wasm) | m=64MB, t=3, p=4, hashLength=32 |
| 旧版兼容 | PBKDF2-SHA256 | 600,000 iterations(可读旧备份) |
| 内容加密 | AES-256-GCM | 96-bit IV, 128-bit tag |
| 同步链路 | ECDH(X25519) + Ed25519 + AES-GCM | 端到端 |
| 备份导出 | argon2id + AES-GCM | 双算法兼容头(algo byte:0x01=PBKDF2 / 0x02=argon2id) |

### 6.2 防护措施

- **主密码**:本机不存储,仅以 argon2id 派生形式保存验证器(`2:saltHex:keyHex` 格式)
- **修改主密码**:设置页"安全"分区可在线修改主密码 — 验证旧密码 → 生成新 verifier 覆盖持久化 →
  清空 derivedKeyCache。校验:新密码 ≥8 位 + 不与旧密码相同 + 两次输入一致。注意:此前生成的
  加密备份仍需用旧密码恢复,新密码只保护此后新生成的备份。
- **密码错误 cooldown**:连续 3 次错误密码后强制 30 秒 cooldown,期间拒绝任何解锁尝试,
  防止暴力枚举。`failedAttempts` + `lockedUntil` 双字段记录状态,成功解锁后清零。
- **锁屏**:可设置 关闭 / 1 / 5(默认) / 15 / 30 / 60 分钟自动锁定,或窗口失焦立即锁。
  设为"关闭"时仅手动锁定(顶部按钮或 Mod+L)。
- **隐私模式**:隐藏保险库内容,只在密码二次确认后显示
- **剪贴板自动清除**:复制保险库密码后默认 30 秒自动清除剪贴板(可在安全设置中调整时长)。
  复制时按钮显示倒计时,模块级 timer 共享,只清最后一次复制的字段。
- **本地敏感词扫描(特定地区版本,规划中)**:仅本地扫描提示,不上云、不上传、不阻断
- **危险区数据销毁**:
  - **清空数据**:删除全部任务 / 保险库 / 项目 / 自定义分类与标签 / 搜索历史 / 同步设备身份,
    **保留主密码与已生成备份文件**,下次启动仍用当前密码解锁。
  - **恢复出厂设置**:在清空数据基础上额外删除主密码 + 安全设置,1 秒后自动 reload 回到首次
    安装状态。**已生成的加密备份文件不受影响**,仍可用旧密码恢复。两者均需二次确认。

### 6.3 安全审计

- 持续运行 gitleaks 密钥扫描、OSSF Scorecard 监控
- 已完成两轮安全审计(36 项发现),追踪见 `docs/security/SECURITY_TRACKER.md`
- 漏洞报告流程见 `SECURITY.md`

### 6.4 数据泄露响应

发现泄露后 72 小时内通知监管机构(GDPR 第 33 条),并在合理时间内通知受影响用户。

---

## 7. 未成年人保护

### 7.1 年龄门槛

| 地区 | 适用年龄 | 要求 |
|---|---|---|
| 美国 | < 13 岁 | 父母可验证同意(COPPA) |
| 欧盟 | < 16 岁(成员国可下调至 13) | 父母同意(GDPR Article 8) |
| 适用 PIPL 地区 | < 14 岁 | 父母实名同意 + 单独《儿童隐私政策》 |
| 其他 | < 13 岁 | 拒绝注册 |

### 7.2 未成年人数据最小化

- 不启用 emotion 字段
- 不启用生灵训练(权重不积累)
- 不启用继承胶囊(法律主体资格问题)
- 默认 L1 标准账号,不主动引导升级 L3 高安全模式

### 7.3 关于"工具属性"叙事

Goto 在所有适用《未成年人保护法》的地区定位为 **"任务管理 + 加密 + 时间资产"** 工具,不构成"网络游戏"。具体合规评估在 Phase A 末前由法务出具意见书;若不确定则按更严格标准申报。

---

## 8. 数据保护官(DPO)与联系方式

> GDPR 第 37(1)(b) 条对处理特殊类别数据(心理 / 情绪 / 健康)无用户量门槛,必须指定 DPO。

| 角色 | 联系方式 | 响应时间 |
|---|---|---|
| **DPO(数据保护官,Phase A 即指定)** | `privacy@goto.app`(待启用) / 当前通过 `SECURITY.md` 中的 maintainer 渠道 | 7 个工作日内 |
| 法务咨询 | 外包欧盟合规咨询公司(Phase A 签约) | — |
| DPIA 报告归档 | `docs/privacy/DPIA_v1.md`(Phase B' 末发布) | — |

如对个人数据处理有疑问,请通过以上渠道联系。如对处理有异议,可向所在地数据保护监管机构投诉。

---

## 9. 变更通知

本政策变更将通过应用内通知 + GitHub Release Notes 公示,重大变更(法律基础、跨境、保留期)提前 30 天通知。用户在变更生效后继续使用即视为接受。

---

## 10. 开源代码透明性

Goto 是开源项目(仓库 `gitcode.com/badhope/goto`),全部数据处理代码可审计:

- 加密实现:`desktop/src/renderer/lib/webAPI.ts`
- 同步协议:`desktop/src/shared/sync/`
- 派生计算:`desktop/src/shared/mosaic/`
- 备份加解密:`desktop/src/renderer/lib/backupCrypto.worker.ts`

用户或第三方安全研究员可随时审计、复现、验证。

---

## 附录 A:GDPR 自评表(Phase A 阶段)

| 评估项 | 状态 |
|---|---|
| 数据控制者 / 处理者识别 | ✅ 已识别(§1) |
| 处理活动记录(第 30 条) | ✅ 已记录(§2 / §4) |
| 法律基础识别(第 6 / 9 条) | ✅ 已识别(§4) |
| 用户权利落地(第 12-22 条) | ✅ 已落地(§3) |
| DPO 指定(第 37 条) | 🔄 Phase A 末完成(§8) |
| DPIA(第 35 条) | ⏳ Phase B' 末完成 |
| 数据泄露响应(第 33 条) | ✅ 已规划(§6.4) |
| 跨境传输(第 44-49 条) | ✅ 已声明(§5.2) |

---

## 附录 B:版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| 0.1(Phase A) | 2026-07-19 | 初始版本,覆盖本地优先架构、用户权利、加密方案、未成年人保护 |
| 1.0(Phase D 上线版) | TBD | 完成 DPIA、最终 DPO 指定、各区域版本落地 |
